git config --global user.email "mastermathstwelve@gmail.com"  
 git config --global user.name "zafaralichattha"                                 
 git add <filename.extension>
 git add .
 git init
 git commit -m "This is my first python  program"
 [
git remote add origin https://github.com/zafaralichattha/test.git [to connect github repo with local repo]
 git remote -v [to view remote origin]
git remote remove origin[to remvoe remote origin]
]
 git branch -M main [rename the branch]
 git push -u origin main  [push to github repo with saving the link for next time using -u]
 git push origin main   [push without saving origin  for next work]

[
git pull origin main --allow-unrelated-histories [adding other files form remote main to local main]
 A vim file will be opened use esc key then :wq to quit it 
]

  git restore <filename.extension>
git rm --cached <filename.extension>
git remote set-url origin https://github.com/zafaralichattha/python.git (to change the main folder)

[git commit --amend --no-edit
git push origin main  --Force(go with previous comments)]

[Get-ChildItem -Force
Remove-Item -Recurse -Force .git (for uninit files)]

git branch branch-name
git checkout branch (switching to other command)
